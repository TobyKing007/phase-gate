public class FunctionTasks      {

    public static int[] adjustArayLength (int[] number, int digit)        {

    if (digit < 0)      {

        break;

            }

    if (digit > number.length)      {

    int[] newArray = new int [digit];

    for (int count = 0; count < number.length; count++)        {

      newArray[count] = number[count];

                }

    for (int counter = number.length; counter < newAray.length; counter++)      {

        newArray[count] = -1;
                    }

                }

        return newArray;

        else if (digit < number.length)     {

            int[] newArray = new int[digit];

            for (int count = 0; count < number.length; count++)     {

                

                

                        }

        


                }

            }

        }
